# Tech Radar — 2026-06-14 (weekly cycle 5)

> Sprint focus 계속: Whale = The Product (W1 front door · W2 reliability · W3 distribution · W5 compliance).
> 본 cycle 머지: PR #152 (growth/W3 WhaleFollow insider-related + 홈 hero Telegram/RSS pill) + PR #153 (defensive/W6 sourceCounts API).
> 후보는 cycle 5 의 *자연스러운 다음 step* — KPI enabler PR #153 의 활용 + cycle 4 미해결 항목 정리.

## 1. `sourceCounts` top-N cap — DoS hedge (W6 defensive follow-up)

**무엇**: PR #153 의 `/api/analytics/summary?window=N` 응답에 노출되는 `sourceCounts` 는 현재 모든 source 키 출력 (sum > 0 인 것만). SEC reviewer 가 명시: "키 수 10개 초과 성장 시 응답 크기 상한(예: top-50 cap)을 명시적으로 두는 후속 가드 권장 — 현 PR 차단 사유 아님". UTM source 화이트리스트가 `[a-z0-9_-]{1,20}` 로 source 1개당 ≤ 20바이트, 현재 9-10종이라 응답 ≤ KB 단위.

대안: `sourceCounts` 출력 시 sort by count DESC + top 50 만 출력. 또는 `?source=whale_*` prefix 필터 옵션 추가.

**왜 지금**: PR #152 머지로 source 7종 → 9-10종 성장. 추가 channel (예: 향후 `/whale/tg-bot-deeplink`, OG image clicks 등) 이 누적되면 10년 후 source 수 폭발 가능성 — proactive guard.

**도입 비용**: `scripts/lib/analytics-summary.js` 5 LOC (sort + slice). 0.2 sprint.

**위험**: cap 누락된 source 가 누락 보임 — 단, 14일 KPI 분석 사이클에서는 top-50 이면 충분.

**결정**: 즉시 미실행. **PR #153 의 14일 KPI 데이터 후 source 수가 30+ 이면 채택**. 그 전까지 holdable.

## 2. `whale_insider` UTM 신호 확인 시 `/whale/insider/<key>` aggregation 페이지 — W2 evidence + W1 SEO (growth follow-up)

**무엇**: PR #152 의 WhaleFollow insider-related aside 는 현재 데이터 (58 insider 중 2 multi-trade) 에서 mostly dormant. 그러나 클릭 데이터가 누적되면 (`whale_insider` UTM source) 인물별 *aggregation 페이지* 가 demand 있음을 확인 가능 — 클릭 ≥ 3건 = 가설 확인.

확인 시: `/whale/insider/<key>/` 동적 페이지 (`getStaticPaths` 로 whale-track-record.json 의 모든 insider 생성) — 그 인물의 모든 trade 시간순 리스트 + D+30/90 트랙레코드 + Tier-1 (≥5 trades, ≥0.65 winrate D+90) 배지. OKR O2.KR2 (`tier1_rendered = 0 → 1`) 의 본 실행.

**왜 지금**: cycle 4 tech-radar 3 항목 (Tier-1 render) 의 *demand 검증 가설* 이 PR #152 로 측정 가능해짐. 단, 데이터 부족 — 현 데이터로 페이지 생성 시 빈 페이지 다수.

**도입 비용**: 1-1.5 sprint. `src/pages/whale/insider/[key].astro` 신규 + getStaticPaths + WhaleFollow.astro 의 aside 링크 업데이트. 인물별 페이지 최대 58개.

**위험**: insider 별 D+90 데이터 부족 시 페이지가 정보 빈약 → noindex 처리 필요. Tier-1 정의 사용자 합의 (cycle 4 tech-radar 3 = 보류) 선행.

**결정**: **PR #152 의 14일 KPI 평가 + 사용자 Tier-1 정의 합의 후 채택**. 약 cycle 7-8 (2026-06-25 전후).

## 3. (cycle 4 항목 1 갱신) Telegram push-rate guardrail — W2 reliability

**상태 update**: cycle 4 의 PR #146 (15분 polling) 머지 후 24시간 경과. burst 데이터 부족 — daily snapshot 1회 (2026-06-14) 만 누적됨.

**다음 단계**: 추가 7일 데이터 (2026-06-14 → 2026-06-21) 후 재평가. cycle 6-7 weekly 에서 결정.

## 결정 권장 요약

| 항목 | 결정 | 이유 |
|---|---|---|
| Tech 1 (top-N cap) | 보류 — source 30+ 도달 시 채택 | proactive, 현재 위험 0 |
| Tech 2 (insider 페이지) | 보류 — PR #152 KPI + Tier-1 합의 후 | cycle 7-8 진입 후보 |
| Tech 3 (push-rate guardrail) | 보류 — 추가 7일 데이터 | cycle 6-7 재평가 |

본 cycle 의 *외부 결정 사항* 없음. **3개 모두 데이터/합의 대기** — 안정적 상태.

---
_Generated by /weekly cycle 5 (2026-06-14). Slack 알림: SLACK_WEBHOOK_URL 미설정 — 알림 생략._
