# EconPedia Improvement Plan — 2026-05-18

*3-Persona synthesis (Musk + Munger + McKinsey). API 키 미설정으로 Claude가 직접 수행.*

---

## Decision: This Cycle's Focus

**신뢰 기반 회복 (Trust Foundation)**

POLLS_FILE/WALLETS_FILE 데이터 손실 버그와 프로덕션 가짜 지표를 이번 주 안에 완전 제거한다. 새 기능 추가 없이, 존재하는 것이 올바르게 동작하고 올바른 데이터를 보여주도록 만든다.

근거: wallet_authenticated_users = 0, 모든 KPI = null인 상태에서 새 기능은 낭비다. P0 버그가 이틀 뒤 deadline인데 미수정 상태는 프로세스 고장 신호다.

---

## Two-week Sprint Plan

### Week 1 (2026-05-18 ~ 2026-05-24) — P0/P1 완전 제거

**Deliverable 1: P0 데이터 손실 버그 수정 (2026-05-20 deadline)**
- `api/server.js` 상단에 `POLLS_FILE`, `WALLETS_FILE` 상수 추가
- `DATA_DIR` 하위의 `polls.json`, `wallets.json`으로 정의
- 1-파일 PR → CI → auto-merge

**Deliverable 2: 가짜 지표 3개 삭제 (2026-05-24)**
- `api/server.js:295` — `openRate` fabricated 계산 제거
- `api/server.js:296` — `rating = 4.8` hardcode 제거
- `api/server.js:305` — `course_completions` 추산 제거
- 삭제 후 해당 필드를 응답에서 빼거나 `null` + 명시적 disclaimer로 교체

**Deliverable 3: `/api/poll/*` rate limit 연결 (2026-05-24)**
- 이미 `isRateLimited(ip)` 함수 구현됨 (api/server.js:150-161)
- `/api/poll/*`, `/api/wallet-subscribe` 핸들러에 `isRateLimited` 체크 추가

### Week 2 (2026-05-25 ~ 2026-05-31) — 보안 + KPI 측정

**Deliverable 4: XSS 수정 — TimeAttackLounge.astro:253**
- Supabase `full_name` 필드를 HTML에 삽입 전 escape 처리

**Deliverable 5: Telegram CTR 측정 시작**
- whale-analysis 생성 시 UTM 파라미터 추가 (`?utm_source=telegram`)
- 클릭 추적으로 실제 CTR 가시화 (KPI: whale_telegram_ctr)

---

## Success Criteria

| KPI | 현재 | 목표 (2주 후) |
|---|---|---|
| fake_metrics_count | 3 | 0 |
| p0_data_loss_bugs | 1 | 0 |
| /api/poll/* rate limited | ✗ | ✓ |
| whale_telegram_ctr | null | 측정 시작 |
