# Tech Radar — 2026-06-18 (weekly cycle 6)

> Sprint focus 계속: Whale = The Product (W1 front door · W2 reliability · W3 distribution · W5 compliance).
> 본 cycle 머지: PR #157 (defensive/W6 insider 정규화 단일 소스) + PR #158 (growth/W3 Article rich-result + OG article:* 메타).
> Cycle 5 의 "데이터/합의 대기 3건" 상태가 동일하게 유지 — 본 cycle 의 *직접 다음* 후보 1-2개.

## 1. `JSON.stringify` 의 `</script>` break-out hardening — W5 defense-in-depth

**무엇**: cycle 6 PR #158 의 SEC reviewer 가 명시: "`set:html={JSON.stringify(...)}` 는 변경 전부터 존재. `title`/`description` 에 literal `</script>` 가 들어가면 break-out 이론상 가능. 현재 호출처 전수 정적 — 신규 회귀 0. 그러나 `JSON.stringify(...).replace(/</g,'\\u003c')` 한 줄 hardening 권고 — 별도 PR".

**왜 지금**: Whale Alert pipeline 의 `generate-whale-analysis.js` 가 Claude/Gemini 분석 결과를 title/description 으로 직접 사용 — LLM 출력에 `</script>` 가 포함될 확률은 낮지만 0 은 아님 (PII 회피·환각 방지가 우선이라 escape 처리 안 됨). 단일 1-line 변경 + JSON 파싱 영향 0.

**도입 비용**: `src/layouts/BaseLayout.astro` 1 line (`.replace(/</g, '\\u003c')`). 0.05 sprint.

**위험**: 0 — `<` 는 JSON.parse 시 `<` 로 복원, JSON-LD 파서 영향 없음. Schema.org 인식 영향 0.

**결정**: 다음 daily 사이클 또는 cycle 7 의 defensive 슬롯으로 채택 권장 — SEC reviewer 의 명시적 hardening 권고이므로 우선순위 ↑.

## 2. Google Search Console 가입·sitemap 제출 상태 점검 — PR #158 KPI 측정 전제 조건

**무엇**: PR #158 의 Article rich-result 효과를 *측정하려면* Google Search Console (GSC) 가 econpedia.dedyn.io 도메인을 인덱싱 중이어야 함. 현재 GSC 등록 여부 + sitemap.xml 제출 상태 미확인.

대안: 사용자 컨펌 — 이미 GSC 등록되어 있다면 단순 URL Inspection (3 샘플 whale 페이지) 으로 14일 후 검증. 미등록이라면 등록 + sitemap 제출 (BaseLayout 에 이미 `astro-sitemap` integration 으로 `sitemap-index.xml` 생성됨).

**왜 지금**: PR #158 의 KPI 가설("14-30일 후 rich-result 자격") 검증 불가능 시 본 growth bet 전체가 측정 부재 → 학습 0.

**도입 비용**: GSC 가입 1회 (사용자 액션) + sitemap 제출 (URL 입력 1건). 코드 변경 0.

**위험**: 0 — GSC 등록은 모니터링 채널, 콘텐츠 노출 영향 없음.

**결정**: **사용자 결정 대기** — daily/weekly 다음 사이클에서 GSC 상태 확인 후 cycle 7 의 measurement step 으로 진입.

## 3. (cycle 4-5 항목 갱신) sourceCounts top-N cap · push-rate guardrail · Tier-1 insider 페이지

**상태 update**:
- **top-N cap** (cycle 4-5 holdable): UTM source 화이트리스트 현재 9-10종. PR #152 의 14일 KPI 데이터 누적 후 (목표 2026-06-28 전후) 재평가.
- **push-rate guardrail** (cycle 4): PR #146 의 15분 polling 머지 후 96시간 경과 (cycle 4 → cycle 6). burst 데이터 부족 — 추가 5-7일 후 cycle 7-8 재평가.
- **Tier-1 insider 페이지** (cycle 4-5): PR #152 의 `whale_insider` UTM 신호 ≥ 3건 + 사용자 Tier-1 정의 합의 후. 2026-06-25 전후 cycle 7-8 후보 유지.

본 cycle 머지로 *새로 unlocked 된 후보 없음* — 기존 3건은 자연 데이터 누적/사용자 합의 대기.

## 결정 권장 요약

| 항목 | 결정 | 이유 |
|---|---|---|
| Tech 1 (`</script>` hardening) | **채택** — cycle 7 defensive 슬롯 | SEC reviewer 명시 권고, 1-line + 위험 0 |
| Tech 2 (GSC 가입 점검) | **사용자 결정 대기** — daily 사이클로 확인 | PR #158 KPI 측정 전제 |
| Tech 3 (cycle 4-5 holdable 3건) | 동일 holdable | 데이터/합의 대기 지속 |

본 cycle 의 *외부 결정 사항* 1건 (GSC 가입 확인). **사용자 컨펌 후** Tech 2 진행. Tech 1 은 cycle 7 의 defensive 으로 자동 진입 권장.

---
_Generated by /weekly cycle 6 (2026-06-18). Slack 알림: SLACK_WEBHOOK_URL 미설정 — 알림 생략._
